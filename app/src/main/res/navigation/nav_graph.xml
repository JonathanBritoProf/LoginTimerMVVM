<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainFragment">

    <fragment
        android:id="@+id/mainFragment"
        android:name="br.digitalhouse.loginmvvmdh.acess.view.MainFragment"
        android:label="fragment_main"
        tools:layout="@layout/fragment_main" >
        <action
            android:id="@+id/action_mainFragment_to_timerActivity"
            app:destination="@id/timerActivity" />
    </fragment>
    <activity
        android:id="@+id/timerActivity"
        android:name="br.digitalhouse.loginmvvmdh.timer.view.TimerActivity"
        android:label="activity_timer"
        tools:layout="@layout/activity_timer" />
</navigation>